# TLS Certificates (Local Dev)

The bootstrap command generates a self-signed certificate:

- `devkit.crt`
- `devkit.key`

You can replace them with mkcert-generated certificates (recommended).
See `docs/tls.md`.

Do not commit real certificates.
